<?php
  $url = $this->recordLinker()->getUrl($this->driver);
  $url = isset($info['baseUrl']) ? ($info['baseUrl'] . $url) : $this->serverUrl($url);
?>
UL <?= $url ?>
<?php
// Record separator:
echo "\n";
?>

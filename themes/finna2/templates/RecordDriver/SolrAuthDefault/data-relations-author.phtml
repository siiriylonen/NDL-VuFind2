<?php
  $helper = $this->record($this->driver);
  $searchAction = $this->searchOptions(DEFAULT_SEARCH_BACKEND)->getSearchAction();
?>
<?php foreach ($data as $relation): ?>
  <?=$helper->getLinkedFieldElement('author', $relation['name'], $relation, ['role' => true, 'authorityType' => $relation['type'] ?? null, 'searchAction' => $searchAction])?><br>
<?php endforeach; ?>

<?php
// Use echo to ensure we don't return a line feed
echo $this->url($this->searchAction ?: $this->searchOptions($this->driver->getSourceIdentifier())->getSearchAction(), [], ['query' => ['filter[]' => $this->filter]]);

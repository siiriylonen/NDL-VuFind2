<?php
if (!($id = $this->id ?: $this->localId)) {
    return;
}
// Use echo to ensure we don't return a line feed
echo $this->url(
    $this->searchAction ?: $this->searchOptions($this->driver->getSourceIdentifier())->getSearchAction(),
    [],
    ['query' => ['filter[]' => sprintf('%s:"%s"', \Finna\Search\Solr\AuthorityHelper::AUTHOR2_ID_FACET, $id)]]
);

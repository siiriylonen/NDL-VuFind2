<?php //Don't add START and END comments ?>
<?php
    if (empty($data) || (!is_array($data))) {
        return;
    }
?>
<div class="truncate-field" data-rows="5">
    <?php foreach ($data as $line): ?>
        <?php if (!empty($line['make_model'])): ?>
            <?=$this->escapeHtml($line['make_model']) ?> <br>
        <?php endif; ?>
    <?php endforeach; ?>
</div>
